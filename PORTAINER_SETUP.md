# Настройка Portainer для автоматического деплоя

Это руководство покажет, как настроить Portainer для автоматического деплоя проекта persfin из GitHub репозитория.

## 🎯 Преимущества этого подхода

- ✅ Деплой одной кнопкой из UI Portainer
- ✅ Автоматическое отслеживание изменений в Git
- ✅ Не нужно SSH доступа к серверу
- ✅ История всех деплоев
- ✅ Откат к предыдущим версиям
- ✅ Webhook для автоматизации

---

## 📋 Предварительные требования

1. **Portainer установлен и доступен** (например, через CloudPub)
2. **Доступ к вашему Portainer UI**
3. **GitHub Personal Access Token** (для приватных репозиториев)

---

## 🔑 Шаг 1: Создание GitHub Personal Access Token

Для приватного репозитория Portainer нужен токен для доступа к GitHub.

### 1.1. Создайте токен на GitHub:

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Настройте токен:
   - **Name**: `Portainer persfin`
   - **Expiration**: `No expiration` (или выберите срок)
   - **Scopes**: отметьте только `repo` (полный доступ к приватным репозиториям)
4. Нажмите **"Generate token"**
5. **Скопируйте токен** (он больше не будет показан!)

Пример токена: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

## 🚀 Шаг 2: Создание Stack в Portainer из Git

### 2.1. Откройте Portainer UI

Зайдите в ваш Portainer (например, через CloudPub tunnel).

### 2.2. Выберите Environment

1. Перейдите в **Environments**
2. Выберите ваш Docker environment (где будет запускаться приложение)

### 2.3. Создайте новый Stack

1. В боковом меню выберите **Stacks**
2. Нажмите **"+ Add stack"**

### 2.4. Настройте Stack из Git Repository

Заполните форму:

#### **Name**
```
persfin
```

#### **Build method**
Выберите: **☑️ Git Repository**

#### **Repository URL**
```
https://github.com/akimovdo/persfin
```

#### **Repository reference**
```
refs/heads/main
```

#### **Compose path**
```
docker-compose.yml
```

#### **Authentication** (для приватного репозитория)
- ☑️ Enable: **Yes**
- Username: `akimovdo` (ваш GitHub username)
- Personal Access Token: вставьте токен из Шага 1 (`ghp_xxx...`)

#### **Environment variables** (опционально)
Можно переопределить переменные из docker-compose.yml:

| Name | Value |
|------|-------|
| `PB_PUBLIC_URL` | `https://persfin.42bureau.ru` |
| `PP_HOST` | `https://persfin.42bureau.ru` |

*(Но если они уже в docker-compose.yml, можно оставить пустым)*

#### **Auto update** (важно!)
- **☑️ Enable GitOps updates**: Если включить, Portainer будет автоматически проверять Git и обновлять при изменениях
- **Polling interval**: например, `5m` (каждые 5 минут)

*(Рекомендую пока оставить выключенным и обновлять вручную)*

### 2.5. Deploy Stack

Нажмите **"Deploy the stack"**

Portainer:
1. Склонирует репозиторий
2. Прочитает `docker-compose.yml`
3. Соберёт Docker образ из `Dockerfile`
4. Запустит контейнер

---

## 🔄 Шаг 3: Обновление приложения после изменений

### Способ 1: Вручную через UI (рекомендуется)

1. Откройте **Stacks** → **persfin**
2. Нажмите **"Pull and redeploy"**
3. Подтвердите действие

Portainer:
- Сделает `git pull`
- Пересоберёт образ
- Перезапустит контейнер

### Способ 2: Автоматически (GitOps)

Если включили **"Enable GitOps updates"**, Portainer сам будет проверять Git каждые N минут и обновляться при изменениях.

### Способ 3: Через Webhook (для CI/CD)

#### 3.1. Создайте webhook в Portainer:

1. Откройте **Stacks** → **persfin**
2. Найдите секцию **"Webhooks"**
3. Нажмите **"+ Add webhook"**
4. Выберите тип: **"Redeploy the stack"**
5. Скопируйте Webhook URL

Пример: `https://portainer.42bureau.ru/api/stacks/webhooks/xxxxxxxxx`

#### 3.2. Используйте webhook:

```bash
# Вручную через curl
curl -X POST https://portainer.42bureau.ru/api/stacks/webhooks/xxxxxxxxx

# Или из GitHub Actions
```

---

## 🔧 Шаг 4: Мониторинг и управление

### Просмотр логов

1. Откройте **Stacks** → **persfin**
2. В списке контейнеров найдите `persfin`
3. Нажмите на имя контейнера
4. Перейдите на вкладку **"Logs"**

Или нажмите **"View logs"** прямо из списка контейнеров в Stack.

### Перезапуск контейнера

1. **Stacks** → **persfin**
2. Найдите контейнер `persfin`
3. Нажмите **"Restart"**

### Остановка/Удаление Stack

1. **Stacks** → **persfin**
2. Нажмите **"Stop this stack"** или **"Delete this stack"**

---

## 📊 Шаг 5: GitHub Actions для автоматического webhook (опционально)

Если хотите автоматический деплой при `git push`:

Создайте `.github/workflows/portainer-deploy.yml`:

```yaml
name: Deploy to Portainer

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Portainer Webhook
        run: |
          curl -X POST ${{ secrets.PORTAINER_WEBHOOK_URL }}
```

**Настройка:**
1. GitHub → Settings → Secrets → Actions
2. Добавьте секрет `PORTAINER_WEBHOOK_URL` с URL webhook из Portainer

**Теперь:**
```bash
git push
```
→ GitHub Actions → Portainer Webhook → Автоматический деплой! 🎉

---

## 🐛 Решение проблем

### Ошибка: "Failed to clone repository"

**Причина:** Неправильный токен или репозиторий

**Решение:**
1. Проверьте URL репозитория: `https://github.com/akimovdo/persfin`
2. Проверьте токен (должен начинаться с `ghp_`)
3. Убедитесь что токен имеет `repo` scope
4. Попробуйте создать новый токен

### Ошибка: "Build failed"

**Причина:** Ошибка в Dockerfile или отсутствие зависимостей

**Решение:**
1. Проверьте логи сборки в Portainer UI
2. Убедитесь что локально `docker build .` работает
3. Проверьте что все файлы закоммичены в Git

### Контейнер не запускается

**Причина:** Ошибка в docker-compose.yml или runtime ошибка

**Решение:**
1. Проверьте логи контейнера в Portainer
2. Убедитесь что переменные окружения правильные
3. Проверьте что порт 8080 не занят

### pb_data теряется при обновлении

**Причина:** Volume не сохраняется

**Решение:**
В docker-compose.yml volume настроен как `./pb_data:/pb/pb_data`. 

Portainer создаст **named volume** автоматически. Данные сохраняются между обновлениями.

Проверьте в Portainer: **Volumes** → найдите volume с именем `persfin_pb_data`

---

## ⚡ Быстрая справка: Команды для деплоя

| Действие | Как сделать |
|----------|-------------|
| **Первый деплой** | Portainer → Stacks → Add stack → Git Repository |
| **Обновить код** | Portainer → Stacks → persfin → Pull and redeploy |
| **Посмотреть логи** | Portainer → Stacks → persfin → View logs |
| **Перезапустить** | Portainer → Stacks → persfin → Restart |
| **Автодеплой** | Включить GitOps updates ИЛИ webhook + GitHub Actions |

---

## 🎉 Готово!

Теперь ваш проект настроен на автоматический деплой через Portainer. 

**Workflow:**
1. Делаете изменения в коде локально
2. `git push`
3. Открываете Portainer UI
4. Нажимаете **"Pull and redeploy"**
5. Готово! 🚀

Или настройте webhook для полностью автоматического деплоя при push.
