# Docker & CloudPub Workflow

## 🚀 Первоначальная настройка проекта

### На любом компьютере:

```bash
# Клонировать проект
git clone git@github.com:akimovdo/persfin.git
cd persfin

# Если нужны данные - скопировать pb_data
# (через архив, rsync, или создать пустую папку для новой БД)

# Отредактируйте docker-compose.yml и укажите ваш публичный URL в переменных окружения

# Запустить через Docker Compose
docker compose up -d --build

# Проверить запуск
docker compose logs -f
```

Открыть http://localhost:8080

### Переменные окружения для публичной публикации

В `docker-compose.yml` настроены переменные окружения:

- `PB_PUBLIC_URL`: Публичный URL приложения (например, `https://persfin-abc123.cloudpub.ru`) - для корректной работы CORS, cookies и ссылок в PocketBase.
- `PP_HOST`: Хост для PocketPages API (обычно тот же, что и `PB_PUBLIC_URL`) - для клиентского JS SDK.

**⚠️ Важно:** Замените URL в `docker-compose.yml` на реальный от CloudPub. Без этих переменных могут возникать ошибки CORS и неправильные ссылки.

Для локальной разработки можно оставить эти строки закомментированными или удалить.

---

## 🔄 Workflow при изменении кода

### 1. Внесение изменений

```bash
# Редактируете файлы в pb_hooks, pb_migrations и т.д.
# ... делаете изменения ...

# Коммитите
git add .
git commit -m "Описание изменений"
git push
```

### 2. Применение изменений локально или на сервере

```bash
# Получить обновления
git pull

# Пересобрать и перезапустить
docker compose down
docker compose up -d --build

# Проверить логи
docker compose logs -f
```

Или одной командой:

```bash
git pull && docker compose up -d --build
```



**⚠️ Важно:** pb_data монтируется как volume, поэтому данные НЕ теряются при пересборке!

---

## 🌐 Публикация через Portainer + CloudPub (рекомендуется)

### Автоматический деплой из Git репозитория

**Это самый удобный способ для продакшена!**

Portainer может автоматически деплоить приложение прямо из GitHub репозитория.

**Преимущества:**
- ✅ Деплой одной кнопкой через UI
- ✅ Автоматическое отслеживание изменений в Git
- ✅ Не нужен SSH доступ к серверу
- ✅ История всех деплоев
- ✅ Webhook для автоматизации

**📖 Подробная инструкция:** см. [PORTAINER_SETUP.md](./PORTAINER_SETUP.md)

**Краткий процесс:**
1. Создайте GitHub Personal Access Token
2. В Portainer: Stacks → Add stack → Git Repository
3. Укажите репозиторий: `https://github.com/akimovdo/persfin`
4. После изменений кода: нажмите "Pull and redeploy" в Portainer UI

---

## 🌐 Альтернатива: Публикация через CloudPub Desktop

Если не используете Portainer:

### Вариант 1: Через CloudPub Desktop

1. **Убедитесь что Docker образ собран:**
   ```bash
   docker images | grep persfin
   ```

2. **Откройте CloudPub Desktop**

3. **Создайте новый проект:**
   - Выберите "Deploy from Docker"
   - Выберите образ `persfin:latest`
   - Настройте:
     - **Порт:** 8080
     - **Volume:** `/pb/pb_data` (для сохранения данных)
     - **Домен:** ваш поддомен
     - **Переменные окружения:** Добавьте `PB_PUBLIC_URL` и `PP_HOST`

4. **Запустите деплой**

### Вариант 2: Экспорт образа

```bash
# Экспортировать образ
docker save persfin:latest -o persfin.tar

# Загрузить через CloudPub UI
```

### Обновление после изменений

```bash
# Пересобрать образ локально
docker build -t persfin .

# В CloudPub:
# - Остановить контейнер
# - Обновить образ
# - Запустить контейнер
```

**⚠️ Важно:** При обновлении убедитесь что volume для `/pb/pb_data` сохранён!

---



## 📋 Полезные команды Docker

### Docker Compose команды

```bash
# Запустить контейнеры
docker compose up -d

# Остановить контейнеры
docker compose down

# Перезапустить контейнеры
docker compose restart

# Пересобрать и запустить
docker compose up -d --build

# Посмотреть логи
docker compose logs -f

# Посмотреть статус
docker compose ps

# Остановить и удалить всё (включая volumes - ОСТОРОЖНО!)
docker compose down -v
```

### Управление контейнерами

```bash
# Список запущенных контейнеров
docker ps

# Список всех контейнеров (включая остановленные)
docker ps -a

# Остановить контейнер
docker stop persfin

# Запустить остановленный контейнер
docker start persfin

# Перезапустить контейнер
docker restart persfin

# Удалить контейнер
docker rm persfin

# Удалить контейнер принудительно (если запущен)
docker rm -f persfin
```

### Логи и отладка

```bash
# Посмотреть логи
docker logs persfin

# Логи в реальном времени
docker logs -f persfin

# Последние 100 строк логов
docker logs --tail 100 persfin

# Войти в контейнер (для отладки)
docker exec -it persfin sh
```

### Управление образами

```bash
# Список образов
docker images

# Удалить образ
docker rmi persfin

# Удалить неиспользуемые образы
docker image prune

# Информация об образе
docker inspect persfin:latest
```

### Очистка Docker

```bash
# Удалить все остановленные контейнеры
docker container prune

# Удалить неиспользуемые образы
docker image prune

# Удалить всё неиспользуемое (контейнеры, образы, сети, volumes)
docker system prune

# Удалить всё включая volumes (ОСТОРОЖНО!)
docker system prune -a --volumes
```

---

## 🔧 Режим разработки (hot reload)

Для быстрой разработки можно монтировать код напрямую:

```bash
docker run -d \
  -p 8080:8080 \
  -v "$(pwd)/pb_data:/pb/pb_data" \
  -v "$(pwd)/pb_hooks:/pb/pb_hooks" \
  -v "$(pwd)/pb_migrations:/pb/pb_migrations" \
  --name persfin-dev \
  persfin
```

**Плюсы:** Изменения в pb_hooks видны сразу (после перезагрузки страницы)  
**Минусы:** Для миграций всё равно нужен restart контейнера

---

## 💾 Резервное копирование

### Бэкап данных

```bash
# Создать архив pb_data
tar -czf pb_data_backup_$(date +%Y%m%d).tar.gz pb_data/

# Или через Docker (если контейнер запущен)
docker exec persfin tar -czf /tmp/backup.tar.gz -C /pb pb_data
docker cp persfin:/tmp/backup.tar.gz ./pb_data_backup.tar.gz
```

### Восстановление из бэкапа

```bash
# Распаковать архив
tar -xzf pb_data_backup_20251112.tar.gz

# Перезапустить контейнер с восстановленными данными
docker restart persfin
```

---

## ⚠️ Важные моменты

1. **pb_data всегда монтируется как volume** - данные сохраняются между пересборками
2. **При обновлении кода** - используйте `git pull && docker compose up -d --build`
3. **Не делайте git pull внутри контейнера** - это нарушает принципы Docker
4. **На CloudPub** - укажите правильный публичный URL в `docker-compose.yml`
5. **Регулярно делайте бэкапы** pb_data
6. **Для локальной разработки** - можно закомментировать переменные окружения в `docker-compose.yml`

---

## 🐛 Решение проблем

### Контейнер не запускается

```bash
# Посмотреть логи
docker logs persfin

# Посмотреть детали контейнера
docker inspect persfin
```

### Порт 8080 занят

```bash
# Найти что использует порт
lsof -i :8080

# Или использовать другой порт
docker run -d -p 8090:8080 -v "$(pwd)/pb_data:/pb/pb_data" --name persfin persfin
```

### Ошибки миграций

```bash
# Проверить что pb_data правильно примонтирован
docker exec persfin ls -la /pb/pb_data

# Проверить логи для деталей ошибки
docker logs persfin
```

### Образ слишком большой

```bash
# Посмотреть размер образа
docker images persfin

# Пересобрать без кеша
docker build --no-cache -t persfin .
```
